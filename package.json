{
  "name": "fotoczarnik-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Statyczne portfolio fotografii i podróży — treść jako pliki, budowane statycznym generatorem, deployowane na Netlify.",
  "scripts": {
    "build": "node build/build.js",
    "dev": "npm run build && npx --yes serve public -l 4173"
  },
  "dependencies": {
    "fs-extra": "^11.2.0",
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  }
}
